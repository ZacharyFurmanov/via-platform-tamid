// The AI accuracy / training / eval dashboard lives inside the infrastructure
// workspace. It reuses the same component the /admin route served.
export { default } from "@/app/admin/intake-accuracy/page";
